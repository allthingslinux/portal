/**
 * @name COLORS
 * @description Centralized color constants for usage where CSS variables are not available (e.g. Emails, Canvas, etc.)
 * These should match the tokens defined in theme.css
 */
export const COLORS = {
  background: "#fafafa",
  foreground: "#242424",
  primary: "#7aa2f7", // Tokyo Night Blue
  border: "#e1e2e7",
} as const;
